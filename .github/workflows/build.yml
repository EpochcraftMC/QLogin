# ============================================================
# QLogin v1_18_2-fabric standalone build
# Branch: v1.21.1-fabric | Java: 21
# ============================================================
name: Build v1_18_2-fabric

on:
  push:
    branches: [ "main", "v1.21.1-fabric" ]
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'MODRINTH.md'
  workflow_dispatch:

jobs:
  build:
    name: "Build v1_18_2-fabric"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build
        run: ./gradlew :v1_18_2-fabric:build --no-daemon

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: v1_18_2-fabric
          path: v1_18_2-fabric/build/libs/*.jar
          if-no-files-found: warn