# ============================================================
# QLogin v1_21-neoforge standalone build
# Branch: v1.21-neoforge | Java: 21
# ============================================================
name: Build v1_21-neoforge

on:
  push:
    branches: [ "main", "v1.21-neoforge" ]
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'MODRINTH.md'
  workflow_dispatch:

jobs:
  build:
    name: "Build v1_21-neoforge"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build
        run: ./gradlew :v1_21-neoforge:build --no-daemon

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: v1_21-neoforge
          path: v1_21-neoforge/build/libs/*.jar
          if-no-files-found: warn