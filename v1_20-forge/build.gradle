plugins {
    id 'java'
    id 'net.minecraftforge.gradle' version '[6.0.16,6.2)'
}

base {
    archivesName = "${rootProject.archives_base_name}-1.20"
}

version = rootProject.mod_version
group = rootProject.maven_group

java {
    toolchain.languageVersion = JavaLanguageVersion.of(17)
}

minecraft {
    mappings channel: 'official', version: '1.20'
    accessTransformer = file('src/main/resources/META-INF/accesstransformer.cfg')
    runs {
        server {
            workingDirectory project.file('run')
            property 'forge.logging.markers', 'REGISTRIES'
            property 'forge.logging.console.level', 'debug'
            mods {
                qlogin {
                    source sourceSets.main
                }
            }
        }
    }
}

sourceSets {
    main {
        java {
            srcDir "src/main/java"
        }
        resources {
            srcDir "src/main/resources"
        }
    }
}

repositories {
    mavenCentral()
}

dependencies {
    minecraft "net.minecraftforge:forge:${rootProject.forge_1_20}"

    implementation 'org.xerial:sqlite-jdbc:' + rootProject.sqlite_jdbc_version
}

jar {
    manifest {
        attributes([
            'Specification-Title'     : 'QLogin',
            'Specification-Vendor'    : 'aqing',
            'Specification-Version'   : '1',
            'Implementation-Title'    : 'QLogin-1.20',
            'Implementation-Version'  : rootProject.mod_version,
            'Implementation-Vendor'   : 'aqing'
        ])
    }
}

processResources {
    inputs.property "version", rootProject.mod_version
    filteringCharset "UTF-8"
    filesMatching("META-INF/mods.toml") {
        expand "version": rootProject.mod_version
    }
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}

tasks.withType(JavaCompile).configureEach {
    it.options.encoding = 'UTF-8'
    it.options.release = 17
}

// Bundle SQLite driver into jar
jar {
    from {
        configurations.runtimeClasspath.findAll {
            it.name.contains('sqlite')
        }.collect {
            it.isDirectory() ? it : zipTree(it)
        }
    }
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}

tasks.withType(Jar).configureEach {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}